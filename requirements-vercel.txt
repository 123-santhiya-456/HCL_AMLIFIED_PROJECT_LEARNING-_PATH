# ─── Core Backend ───────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
python-dotenv==1.0.1
httpx==0.27.0
requests==2.32.3
SQLAlchemy==2.0.31
aiosqlite==0.20.0
python-multipart==0.0.9

# ─── Data / ML (lightweight) ────────────────────────────────────
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.0
networkx==3.3

# ─── LLM ────────────────────────────────────────────────────────
google-generativeai==0.7.2

# ─── Frontend (not used by Vercel backend, but needed for local) ─
# streamlit==1.36.0   # Cannot deploy on Vercel — use Streamlit Cloud
# plotly==5.22.0
# watchdog==4.0.1

# ─── Heavy ML (use locally or on Render/Railway instead) ─────────
# sentence-transformers==3.0.1   # too large for Vercel (250MB limit)
# faiss-cpu==1.8.0               # too large for Vercel
# transformers==4.42.4           # too large for Vercel
# torch==2.3.1                   # too large for Vercel (>1GB)
