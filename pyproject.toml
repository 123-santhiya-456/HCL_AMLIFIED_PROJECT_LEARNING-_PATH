[tool.vercel]
entrypoint = "backend.main:app"

[project]
name = "learnpath-ai"
version = "1.0.0"
description = "AI-Powered Personalized Learning Path Recommender — HCLTech AMPLIFIED"
requires-python = ">=3.10"
